<li class="reistkaartGroot" data-reis-id="<%= data.reis._id %>">
    <article>
        <img src="/uploads/coverfoto/<%= data.reis.fotoPad %>" alt="<%= data.reis.reisTitel %>">
        <div class="reiskaartInhoud">
            <h2><%= data.reis.reisTitel %></h2>
            <p>
                Tussen 
                <b><time datetime="<%= data.reis.startDatum %>">
                    <%= new Date(data.reis.startDatum).toLocaleDateString('nl-NL', { day: 'numeric', month: 'long', year: 'numeric' }) %>
                </time></b>
                en 
                <b><time datetime="<%= data.reis.eindDatum %>">
                    <%= new Date(data.reis.eindDatum).toLocaleDateString('nl-NL', { day: 'numeric', month: 'long', year: 'numeric' }) %>
                </time></b>
                voor een periode van <b><%= data.reis.aantalDagen %></b> dagen.
            </p>

            <ul class="chipsInfo">
                <li><%= data.reis.bedragen %></li>
                <li><%= data.reis.reizen %></li>
                <li><%= data.reis.verblijf %></li>
                <li><%= data.reis.minLeeftijd %> tot <%= data.reis.maxLeeftijd %> jaar oud</li>
            </ul>
            <p><%= data.reis.reis_samenvatting %></p>

            <h3>Bestemming(en)</h3>
            <ul class="chipsInfo">
                <% if (data.reis.bestemmingen && data.reis.bestemmingen.length > 0) { %>
                    <% data.reis.bestemmingen.forEach(function(bestemming) { %>
                        <li><%= bestemming %></li>
                    <% }) %>
                <% } else { %>
                    <li>Nog geen bestemmingen</li>
                <% } %>
            </ul>
        
            <h3>Activiteiten</h3>
             <ul class="chipsInfo">
                <% if (data.reis.activiteit && data.reis.activiteit.length > 0) { %>
                    <% data.reis.activiteit.forEach(function(activiteit) { %>
                        <li><%= activiteit %></li>
                    <% }) %>
                <% } else { %>
                    <li>Nog geen activiteiten</li>
                <% } %>
            </ul>

            <h3>Reizigers</h3>
            <p><%= data.reis.minReizigers  %> tot <%= data.reis.maxReizigers %> reizigers in totaal</p>

             <div class="reizigerAvatars">
                <img src="/images/default-avatar.svg" alt="organisator">
                <img src="/images/default-avatar.svg" alt="reiziger 1">
                <img src="/images/default-avatar.svg" alt="reiziger 2">
            </div>

            <div>
                <input type="checkbox" id="reisUitklappen" name="Meer lezen">
                <label for="reisUitklappen">Meer lezen</label>
                <p><%= data.reis.reis_beschrijving %></p>
            </div>
        </div>
    </article>
</li>