<% layout('layouts/boilerplate') %>
<main id="main" class="profielBwerken">
    <h1>Profiel aanpassen</h1>
    
    <form action="/profiel/bewerken" method="POST" enctype="multipart/form-data">
        <fieldset>
            <legend>Persoonlijke info</legend>
            
            <label for="voornaam">Voornaam</label>
            <input type="text" id="voornaam" name="voornaam" value="<%= data.gebruiker.voornaam %>">

            <label for="achternaam">Achternaam</label>
            <input type="text" id="achternaam" name="achternaam" value="<%= data.gebruiker.achternaam %>">

            <label for="email">Email</label>
            <input type="email" id="email" name="email" value="<%= data.gebruiker.email %>">

                <label for="geslacht">Geslacht</label>
                <select id="geslacht" name="geslacht" required>
                    <option value="Man" <%= data.gebruiker.geslacht === 'Man' ? 'selected' : '' %>>Man</option>
                    <option value="Vrouw" <%= data.gebruiker.geslacht === 'Vrouw' ? 'selected' : '' %>>Vrouw</option>
                    <option value="Anders" <%= data.gebruiker.geslacht === 'Anders' ? 'selected' : '' %>>Anders</option>
                </select>

            <label for="woonplaats">Woonplaats</label>
            <input type="text" id="woonplaats" name="woonplaats" value="<%= data.gebruiker.woonplaats || '' %>">

            <label for="bio">Over mij (Bio)</label>
            <textarea id="bio" name="bio"><%= data.gebruiker.bio || '' %></textarea>
        </fieldset>

        <fieldset>
            <legend>Profielfoto</legend>
            <input type="file" name="profielfoto">
        </fieldset>

        <fieldset>
            <legend>Wachtwoord wijzigen (optioneel)</legend>
            <p>Laat leeg als je je wachtwoord niet wilt veranderen</p>

            <label for="huidigWachtwoord">Huidig wachtwoord</label>
            <input type="password" id="huidigWachtwoord" name="huidigWachtwoord" placeholder="Huidige wachtwoord">

            <label for="nieuwWachtwoord">Nieuw wachtwoord</label>
            <input type="password" id="nieuwWachtwoord" name="nieuwWachtwoord" placeholder="Nieuwe wachtwoord">
        </fieldset>

        <button type="submit" class="save-btn">Wijzigingen opslaan</button>
        <a href="/profiel" class="cancel-link">Annuleren</a>
    </form>
</main>